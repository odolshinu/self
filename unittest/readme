This is my implementation of unittest in Python.
When we run the unittest.py we get into the module.py file and return executed steps of module.py's functions. The one thing to remember is only those functions are executed which have a function name starting with name 'test'.
